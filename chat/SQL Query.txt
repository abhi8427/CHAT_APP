CREATE TABLE LOGIN (
    -> loginid VARCHAR(10) NOT NULL,
    -> name VARCHAR(30) NOT NULL,
    -> password VARCHAR(10) NOT NULL,
    -> email VARCHAR(30) NOT NULL,
    -> type VARCHAR(10) NOT NULL,
    -> PRIMARY KEY(loginid));


CREATE TABLE LOGIN (loginid VARCHAR(20), name VARCHAR(30),
       password VARCHAR(20), email VARCHAR(20), type VARCHAR(20), PRIMARY KEY(loginid) );


CREATE TABLE CHATROOMS (roomname VARCHAR(20), roomdesc VARCHAR(50), PRIMARY KEY(roomname) );


import javax.servlet.ServletException;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;


select User, Host, Password from mysql.user;

INSERT INTO LOGIN VALUES ('abc001', 'ABHISHEK', 'abc123', 'abc@xyz.com', 'User');

INSERT INTO LOGIN VALUES ('abc002', 'AKSHITA', 'xyz123', 'xyz@abc.com', 'User');